sudo: false
language: node_js
language: node_js
node_js:
  - "0.10"
before_script:
  - gem update --system
  - gem install sass --version "=3.2.12"
  - gem install compass --version "=1.0.3"
  - npm install -g bower
  - bower install
  - npm install grunt-cli -g
